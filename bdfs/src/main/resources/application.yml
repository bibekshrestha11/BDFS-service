server:
  port: 8181
  address: 0.0.0.0

spring:
  application:
    name: bdfs

  datasource:
    url: jdbc:postgresql://localhost:5432/bdfs
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

jwt:
  rsa-private-key: classpath:certs/private_key.pem
  rsa-public-key: classpath:certs/public_key.pem

app:
  filepath:
    windows: D:\\college_hub\\
    linux: /home/user/college_hub/
    mac: /Users/college_hub/

springdoc:
  api-docs:
    path: /v1/api-docs
    enabled: true
  swagger-ui:
    path: /v1/swagger
